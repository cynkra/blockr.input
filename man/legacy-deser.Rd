% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-deser.R
\name{legacy-deser}
\alias{legacy-deser}
\alias{blockr_deser.edit_block}
\alias{blockr_deser.grid_entry_block}
\alias{blockr_deser.table_crud_block}
\title{Legacy deserialization for old block formats}
\usage{
\method{blockr_deser}{edit_block}(x, data, ...)

\method{blockr_deser}{grid_entry_block}(x, data, ...)

\method{blockr_deser}{table_crud_block}(x, data, ...)
}
\arguments{
\item{x, data, ...}{Passed through from \code{\link[blockr.core:blockr_ser]{blockr.core::blockr_deser()}}.}
}
\description{
Restores boards saved before the interactive blocks were flattened. Each
block used to take a single opaque \code{state = list(...)} constructor argument
and serialize it under one \code{state} payload key; they now take flat top-level
arguments (\code{key_col}, \code{upserts}, ...) serialized as flat sibling payload
entries.
}
\details{
This deserializer unwraps the interim single-\code{state} blob back to flat
constructor arguments; current (flat) payloads pass straight through. Block
attributes (\code{block_name}, ...) ride along as siblings of \code{state}, so lifting
only the \code{state} key preserves them. The original serialized class is
restored so blockr.core's post-deser class check passes even when the block
was since renamed (e.g. \code{table_block} -> \code{table_crud_block}).

Drop this file when backwards compatibility is no longer needed.
}
\keyword{internal}
